services:
  user-app:
    container_name:
      user-app
    build:
      ./services/Users
    image:
      user
    working_dir:
      /src
    ports:
      - "8000:8000"
    volumes:
      - ./services/Users/db_data:/src/db_data
      - ./services/Users:/src